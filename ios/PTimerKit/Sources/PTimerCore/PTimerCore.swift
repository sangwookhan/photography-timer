// PTimerCore — reusable, Foundation-only calculation/state engine.
//
// Skeleton placeholder for PTIMER-177 commit 1 (package + API skeleton, no
// behavior change). The engine is extracted into this target in later commits,
// organized by capability: Exposure, Reciprocity, Catalog, Timer,
// PresentationSemantics. PTimerCore must stay Foundation-only — no SwiftUI,
// UIKit, UserNotifications, ActivityKit, or AudioToolbox.
