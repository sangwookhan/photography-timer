<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets the user opt into exact completion alarms (API 31+). Without a
         grant the app falls back to best-effort inexact alarms; usage requires a
         Play Console exact-alarm declaration. USE_EXACT_ALARM is intentionally
         NOT used (eligibility/policy risk). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PTimer">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fires scheduled timer-completion notifications even when the app
             process is not running. Best-effort alarm; exactness policy lives
             in AndroidTimerCompletionScheduler. -->
        <receiver
            android:name=".timer.CompletionAlarmReceiver"
            android:exported="false" />
    </application>

</manifest>
