import XCTest
import PTimerCore

/// Skeleton smoke test for PTIMER-177 commit 1: confirms the PTimerCore module
/// builds, links, and imports off-simulator. Replaced by extracted-engine
/// coverage in later commits.
final class PTimerCoreSkeletonTests: XCTestCase {
    func testModuleImports() {
        XCTAssertTrue(true)
    }
}
