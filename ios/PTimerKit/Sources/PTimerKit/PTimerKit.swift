// Copyright © 2026 Sangwook Han
// SPDX-License-Identifier: Apache-2.0

// PTimerKit — reusable iOS app logic + SwiftUI component kit, built on PTimerCore.
//
// Skeleton placeholder for PTIMER-177 commit 1 (package + API skeleton, no
// behavior change). Models/view-models, presenters, display states, the timer
// runtime + completion seam, and reusable SwiftUI components are moved into
// this target in later commits. PTimerKit may import SwiftUI but must not
// import UIKit, UserNotifications, ActivityKit, or AudioToolbox.
